# 2026-06-27 项目讨论总结

## 一、用户新增搭建原则

用户确认开始搭建 Dao Oracle 网站，并提出以下原则：

- 面向欧美市场。
- 需要部署在海外服务器。
- 用户不懂服务器部署和代码开发，因此需要 Codex 负责代码开发、测试、bug 修复、部署安装实施。
- 网站结构要便于独立站推广，符合现有 SEO/GEO 推广方案，提高权重和收录。
- 后期需要对接支付、大模型深度解读、知识库，让网站具备可学习成长能力。
- 后期逐步实现推广自动化流程。

## 二、已采用的工程原则

本轮搭建按以下方向执行：

- 海外部署优先：Vercel Preview / Production。
- 数据库优先：Supabase Postgres / Auth。
- SEO/GEO 优先：可索引页面、sitemap、robots、内部链接。
- AI 可扩展：固定卦义和模型个性化分析分离。
- 后台可运营：先搭建后台信息架构，后续接真实数据和权限。
- 支付可扩展：第一版不接 Stripe，但保留后续升级边界。

## 三、本轮已完成搭建

新增全栈应用目录：

- `apps/web`

已完成模块：

- Next.js App Router 应用骨架。
- Tailwind CSS 基础视觉系统。
- 英文首页。
- 64 卦 JSONL 数据读取和校验。
- Supabase 初始数据库 migration。
- 64 卦 seed 校验脚本。
- 64 卦 Supabase dry-run 导入脚本。
- 起卦 cast 逻辑。
- AI 解读上下文构建。
- `/api/readings` API 边界。
- `/api/interpretations` API 边界。
- 首批 SEO/GEO 页面。
- `/hexagrams` 64 卦列表。
- 64 个 `/hexagrams/[slug]` 静态详情页。
- `robots.txt`。
- `sitemap.xml`。
- 后台 MVP 页面。
- Vercel 部署配置和说明。

## 四、验证结果

已通过：

- `npm --prefix apps/web run test`
- `npm --prefix apps/web run build`
- `npm --prefix apps/web run lint`
- `npm --prefix apps/web run db:validate-seed`
- `npm --prefix apps/web run db:seed-hexagrams -- --dry-run`
- `node tests/dao-oracle-prototype.test.mjs`

本地 HTTP 验证：

- `/` 返回 200。
- `/hexagrams` 返回 200。
- `/admin` 返回 200。
- `/sitemap.xml` 正常输出。
- `/api/readings` 可以返回 mock reading。
- `/api/interpretations` 可以返回 mock AI 解读。

## 五、当前限制

- 尚未创建/连接 Supabase Staging 项目。
- 尚未配置 OpenAI API key。
- API 当前使用 mock persistence 和 mock interpretation。
- 后台是页面结构 MVP，真实登录、权限、编辑和数据查询待连接 Supabase 后继续。
- Vercel Preview 尚未实际部署，需要用户提供或登录 Vercel/Supabase 配置。

## 六、下一步

建议下一步执行：

1. 创建 Supabase Staging 项目。
2. 配置本地和 Vercel 环境变量。
3. 执行 Supabase migration。
4. 真实导入 64 卦数据。
5. 将 reading API 写入 Supabase。
6. 将 interpretation API 接入 OpenAI。
7. 配置 Supabase Auth 和 admin 账户。
8. 部署 Vercel Preview。

## 七、效果图偏差修正

用户反馈：

- 当前页面和效果图偏差很大。
- 页面有缺失。

根因：

- 第一版优先完成了全栈可运行骨架，没有完整迁移 `docs/design-mockups/dao-oracle-effect-mockups.html` 中确认的 7 个流程页面和手机稿视觉系统。

已修正：

- 首页改成居中手机稿结构，更接近效果图首屏。
- `/reading` 改成问题页结构。
- 新增 `/reading/transition`，用于查看问题提交后的过渡动画状态。
- `/reading/cast` 改成深空宇宙门 / 呼吸感摇卦页。
- `/reading/result/[id]` 改成结果卡、卦象、Cosmic Timing、Human Field 和解锁/分享入口结构。
- 新增 `/reading/payment` 付费详情页。
- 新增 `/reading/share` 分享卡页面。
- 路由测试已覆盖新增页面。

验证：

- `npm --prefix apps/web run test` 通过。
- `npm --prefix apps/web run build` 通过，构建生成 90 个页面。
- HTTP 检查 `/`、`/reading`、`/reading/transition`、`/reading/cast`、`/reading/payment`、`/reading/share` 均返回 200。

仍需继续精修：

- 动画节奏。
- 字体细节。
- 首页与效果图的精确间距。
- 结果页真实数据接入后的排版。
- 付费页后续 Stripe 接入。

## 八、星空感、过渡动画和摇卦交互增强

用户继续反馈：

- 整体背景不够宇宙感。
- 尤其过渡页和摇卦页需要加强星空感觉。
- 过渡动画没有做。
- 摇卦效果没有做。

已修正：

- 在手机稿页面中为 `transition` 和 `ritual` 增加深空星点层。
- 增加星点漂移、星云尘埃呼吸、宇宙门多层呼吸环。
- `/reading/transition` 增加自动切换文案和宇宙门呼吸过渡。
- `/reading/cast` 增加可点击的 6 次摇卦交互。
- 每次点击会触发宇宙门脉冲，并逐步生成一条卦线。
- 第 6 次完成后才允许进入结果页。
- 增加进度条和 `0 / 6` 到 `6 / 6` 状态。

验证：

- `npm --prefix apps/web run test` 通过，当前 7 个测试文件、14 个测试通过。
- `npm --prefix apps/web run build` 通过，90 个页面构建成功。
- `/reading/transition` 返回 200。
- `/reading/cast` 返回 200。

仍需后续继续：

- 把演示线序列替换为真实随机/概率摇卦逻辑。
- 把 6 次摇卦结果传入 `/api/readings` 并生成真实 result id。
- 将 result 页面接入 Supabase 和 AI 解读结果。
